# DataConnect Frontend V14.0 — Backend V2.0.5 Integration

Updated from V13.9.1.

## Changes
- Unified Android API route registry with Backend V2.0.5.
- Updated WebView JavaScript API layer to `/api/v2` endpoints.
- Added health and app-version endpoints.
- Added account settings endpoint.
- Added admin route registry.
- Kept the production API URL configurable; do not hard-code company credentials.

## Production
Replace `https://api.example.com` with the actual HTTPS API URL supplied by the hosting company.
Run the backend health check before release.
