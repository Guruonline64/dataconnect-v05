# V14.2 Fingerprint Login

Fingerprint login is implemented with AndroidX BiometricPrompt. After a successful password login, the user can enable fingerprint login. The authentication token is encrypted with an Android Keystore AES-GCM key requiring biometric authentication for use. Password login remains available as fallback. Logging out removes the biometric credential.
